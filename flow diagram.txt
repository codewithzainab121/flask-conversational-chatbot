        👩‍💻 USER (Browser)
              │
              │ types message
              ▼
     ┌───────────────────────┐
     │   FRONTEND (HTML/JS)  │
     │  index.html (UI)      │
     └───────────────────────┘
              │
              │ 1️⃣ send request (POST /api/chat)
              │   JSON:
              │   {
              │     "message": "hi",
              │     "session": {...}
              │   }
              ▼
     ┌───────────────────────┐
     │    FLASK BACKEND      │
     │   (your Python app)   │
     └───────────────────────┘
              │
              │ 2️⃣ route matched
              ▼
     ┌───────────────────────┐
     │  /api/chat → chat()   │
     └───────────────────────┘
              │
              │ 3️⃣ extract data
              ▼
     message = "hi"
     session = {...}

              │
              │ 4️⃣ call brain
              ▼
     ┌───────────────────────┐
     │   get_response()      │
     │  (logic engine)       │
     └───────────────────────┘
              │
              │ uses:
              │ - detect_intent()
              │ - extract_number()
              │ - session memory
              ▼
     🧠 DECISION MADE:
     reply = "Hello Zainab!"
     updated_session = {...}

              │
              │ 5️⃣ send response
              ▼
     ┌──────────────────────────────┐
     │ JSON RESPONSE TO FRONTEND    │
     │                              │
     │ {                            │
     │   "reply": "...",            │
     │   "session": {...},          │
     │   "menu": {...} (optional)   │
     │ }                            │
     └──────────────────────────────┘
              │
              │ 6️⃣ frontend receives
              ▼
     ┌───────────────────────┐
     │   FRONTEND (JS)       │
     └───────────────────────┘
              │
              │ updates UI:
              │ - shows reply
              │ - saves session
              │ - renders menu (if any)
              ▼
        👩‍💻 USER SEES RESPONSE